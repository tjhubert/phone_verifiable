require 'spec_helper'

describe PhoneVerifiable do
  it 'has a version number' do
    expect(PhoneVerifiable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
